# Supabase — Финальная диагностика

**Дата:** 25.04.2026  
**Время:** 21:58  
**Статус:** ⚠️ Частичное подключение

---

## Результаты тестирования

### ✅ Что работает:

1. **Подключение устанавливается**
   - Host: `aws-1-eu-north-1.pooler.supabase.com:6543`
   - User: `postgres.dnpjcxjjavzjmtfzlrip`
   - PostgreSQL: 17.6

2. **Список таблиц получен**
   - `customers` — 96 записей
   - `lots` — (количество неизвестно)
   - `regions` — (количество неизвестно)

### ❌ Что не работает:

1. **Выгрузка данных**
   - Ошибка: `server closed the connection unexpectedly`
   - Происходит при попытке SELECT данных
   - Соединение обрывается после первого запроса

---

## Диагноз

### Проблема:

Supabase **принимает подключение**, но **закрывает соединение** при попытке выполнить запросы к данным. Это указывает на:

1. **Ограничения проекта**
   - Проект может быть в режиме "paused" (приостановлен)
   - Превышен лимит бесплатного плана
   - Проект помечен для удаления

2. **Проблемы с pooler**
   - Transaction pooler может иметь ограничения
   - Таймауты на длительные запросы
   - Проблемы с конкретным проектом

3. **Состояние базы данных**
   - База существует и содержит данные (96+ записей)
   - Но доступ к данным ограничен

---

## Что удалось выяснить

### ✅ Подтверждено:

- Connection string **правильный**
- Проект **существует**
- База данных **содержит данные**:
  - 3 таблицы (customers, lots, regions)
  - Минимум 96 записей в customers

### ❌ Проблема:

- **Невозможно выгрузить данные**
- Сервер обрывает соединение при SELECT запросах
- Это не проблема кода, а проблема проекта Supabase

---

## Рекомендации

### Вариант 1: Проверить статус проекта (рекомендуется)

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Найдите проект с reference `dnpjcxjjavzjmtfzlrip`
3. Проверьте статус:
   - **Active** — проект работает
   - **Paused** — проект приостановлен (нужно возобновить)
   - **Inactive** — проект неактивен
4. Если проект приостановлен:
   - Нажмите "Resume project"
   - Дождитесь активации (~2 минуты)
   - Повторите попытку подключения

### Вариант 2: Использовать Direct Connection

Попробуйте прямое подключение вместо pooler:

```
postgresql://postgres:[PASSWORD]@db.dnpjcxjjavzjmtfzlrip.supabase.co:5432/postgres
```

Найдите в Dashboard → Settings → Database → Connection string → Direct connection

### Вариант 3: Создать новый проект

Если проект был удален или недоступен:

1. Создайте новый проект в Supabase
2. Создайте таблицы: `alembic upgrade head`
3. Загрузите данные из JSON: `python db/load_multi_regions.py`

### Вариант 4: Продолжить с JSON (рекомендуется)

**Текущее состояние:**
- ✅ 6000 лотов в JSON
- ✅ Dashboard работает
- ✅ Telegram-бот работает
- ✅ Аналитика работает

PostgreSQL не критичен для текущей работы.

---

## Выводы

1. **Connection string правильный** — подключение устанавливается
2. **Проект существует** — найдено 3 таблицы с данными
3. **Проблема на стороне Supabase** — сервер обрывает соединение
4. **Вероятная причина** — проект приостановлен (paused)

---

## Следующие шаги

### Если нужен PostgreSQL:

1. Проверьте статус проекта в Dashboard
2. Возобновите проект если он приостановлен
3. Попробуйте Direct Connection
4. Или создайте новый проект

### Если продолжаете с JSON:

1. ✅ Все уже работает
2. Запустите бота: `python bot/main.py`
3. Запустите dashboard: `streamlit run dashboard/app.py`
4. PostgreSQL настройте позже при необходимости

---

## Технические детали

**Протестированные connection strings:**
- ✅ Transaction pooler (6543) — подключение OK, данные недоступны
- ❌ Session pooler (5432) — connection closed
- ❌ aws-0 варианты — tenant not found

**Обнаруженные таблицы:**
- `customers` — 96 записей
- `lots` — (недоступно)
- `regions` — (недоступно)

**Ошибка:**
```
server closed the connection unexpectedly
This probably means the server terminated abnormally
before or while processing the request.
```

---

## Заключение

Supabase проект **существует и содержит данные**, но **доступ ограничен**. Наиболее вероятная причина — проект приостановлен из-за неактивности или превышения лимитов бесплатного плана.

**Рекомендация:** Проверьте статус проекта в Dashboard и возобновите его, либо продолжайте работу с JSON.

Все основные функции TenderLens работают с JSON без PostgreSQL.
